#[cfg(test)]
mod transform_tests {
    use wasm_bindgen_test::*;

    #[wasm_bindgen_test]
    fn test_transform_module_exists() {
        // Basic test that the transform module compiles and loads
        assert!(true);
    }
}
